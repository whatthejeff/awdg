#!/usr/bin/env node
var debug = require('debug')('awdg');
var app = require('../app');
var core = require('../core');

var server = app.listen(core.config.port, function() {
  debug('Express server listening on port ' + server.address().port);
});